{
	"name": "intellex/data-parser",
	"description": "Easy to use lib to parse arrays into objects",
	"version": "2.0.2-alpha",
	"license": "MIT",
	"minimum-stability": "alpha",
	"authors": [
		{
			"name": "Ivan Sabo",
			"email": "sabo@intellex.rs"
		}
	],
	"scripts": {
		"test": "phpunit --coverage-html tests/_coverage --coverage-filter src/ --colors=always tests"
	},
	"autoload": {
		"psr-4": {
			"Intellex\\DataParser\\": "src\\"
		}
	},
	"autoload-dev": {
		"psr-4": {
			"Intellex\\DataParser\\Tests\\": "tests\\"
		}

	},
	"require": {
		"php": ">=8.1",
		"intellex/naming-convention": "^1.1.0"
	},
	"require-dev": {
		"phpunit/phpunit": "^10"
	}
}
